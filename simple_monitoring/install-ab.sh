# to use apache benchmark you have to install it first
sudo apt-get update
sudo apt-get install apache2-utils
ab -v # this shows if installed

# install gnuplot to display chart :)
# sudo apt-get install gnuplot 
