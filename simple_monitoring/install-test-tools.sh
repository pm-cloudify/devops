# you can use these tools any where:

# to use apache benchmark you have to install it first
echo "installing apache benchmark..."
sudo apt-get update
sudo apt-get install apache2-utils

# install gnuplot to display chart :)
echo "installing gnuplot..."
# sudo apt-get install gnuplot 

echo "installing wrk..."
sudo apt update
sudo apt install wrk

# You have to run these test on target machine:

# use this on your linux machine to stress-ng cpu
# echo "installing stress-ng..."
# sudo apt install stress-ng

# use this on your linux machine to stress memory
# echo "installing stress..."
# sudo apt install stress
